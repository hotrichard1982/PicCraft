# PicCraft 术语表 (Glossary)

> 本文档是 PicCraft 项目的**领域术语表**，不是设计文档、不写实现细节。
> 任何新模块、PR、讨论，都应与这里的术语保持一致。

## 1. 顶层概念 (Top-Level Concepts)

| 术语 | 定义 |
|------|------|
| **视图 (View)** | 应用顶层的 4 个并列工作区之一。同一时刻只有一个视图处于"激活"状态。 |
| **当前目录 (Current Folder)** | 当前浏览视图所打开的磁盘目录。 |
| **队列 (Queue)** | 用户从当前目录中挑出来、准备送去"批量编辑"的图片集合。**仅在批量编辑视图中展示。** |

## 2. 视图 (Views)

应用有 **4 个顶级视图**，全部平级：

| 术语 | 别名（不要混用） | 定义 |
|------|------------------|------|
| **浏览视图 (Browse View)** | ~~查看视图~~ ~~图库~~ ~~资源管理器~~ | 浏览当前目录的图片，缩略图网格 + 全屏看图。**应用启动默认入口**（除非带参启动指定其它视图）。 |
| **单图编辑视图 (Edit View)** | ~~单图视图~~ | 对**一张**图片进行缩放/裁剪/翻转/旋转/保存。 |
| **批量编辑视图 (Batch View)** | ~~批量视图~~ | 对**队列**里的图片统一处理（缩放/压缩/格式转换）。**队列面板只在这里显示。** |
| **设置视图 (Settings View)** | ~~帮助~~ ~~关于~~ | 含 3 个子 Tab：设置 / 帮助 / 关于。 |

### 2.1 设置视图的子 Tab

| 子 Tab | 内容 |
|--------|------|
| **设置 (Settings)** | 关联的图片格式勾选、行为选项 |
| **帮助 (Help)** | 教用户如何使用软件 |
| **关于 (About)** | 版本号、技术栈、开源协议、公司信息 |

## 3. 角色与对象 (Entities)

| 术语 | 定义 |
|------|------|
| **图片 (Image)** | 一张磁盘上的图片文件，路径唯一。 |
| **缩略图 (Thumbnail)** | 图片的缩小版，懒加载生成。 |
| **图片信息 (ImageInfo)** | 一张图片的元数据：宽、高、格式、文件大小、路径。 |
| **队列项 (Queue Item)** | 队列中的一项，对应一张图片。**不存储像素，只存路径**。 |

## 4. 关键操作 (Operations)

| 术语 | 定义 |
|------|------|
| **切换视图 (Switch View)** | 在 4 个顶级视图之间切换。 |
| **加入队列 (Enqueue)** | 把图片从浏览视图送入队列。**只在批量编辑视图的队列面板中生效。** |
| **移出队列 (Dequeue)** | 把图片从队列移除。 |
| **清空队列 (Clear Queue)** | 清空整个队列。 |
| **打开图片 (Open Image)** | 在单图编辑视图中打开某张图片（隐含自动 Enqueue）。 |
| **切换目录 (Switch Folder)** | 改变浏览视图的当前目录。**会清空队列**（与"清空队列"区分）。 |
| **进入全屏 (Enter Fullscreen)** | 在浏览视图中聚焦某张图进入全屏看图。 |
| **退出全屏 (Exit Fullscreen)** | 从全屏看图退回到缩略图网格。`Esc` 触发。 |

> ⚠️ **重要决策**：
> - 队列**不在浏览视图维护**——浏览视图只负责挑图
> - 加入队列需要先切到批量编辑视图，或在浏览视图调用一个"加入队列"动作（见 §5）

## 5. 加入队列的入口 (Enqueue Entry Points)

由于队列面板只在批量编辑视图出现，加入队列的入口有两个：

| 入口 | 描述 |
|------|------|
| **浏览视图 → 右键菜单"加入队列"** | 右键点击单张缩略图或多张选中的缩略图，菜单出现"加入队列"。**唯一入口**，无工具栏按钮。隐含：自动切到批量编辑视图。 |
| **单图编辑视图 → "加入队列并打开下一张"按钮** | 编辑完一张图后可继续处理队列中的下一张。 |

## 5.5 浏览视图交互细节 (Browse View Interaction)

| 项 | 决策 |
|----|------|
| **缩略图默认大小** | 宽 300px |
| **缩略图大小调节** | 双绑：`Ctrl + 滚轮` + `Ctrl + 加号/减号`，步长 10% |
| **底部状态栏** | 单选时显示该图信息（大小/尺寸/创建修改日期/路径），多选时仅显示"已选 N 张"。右侧固定显示目录总数。 |
| **多选交互** | Ctrl+点 = 多选 / Shift+点 = 范围选 / 鼠标框选（Windows 风格，可从缩略图上方开始框选） |
| **拖拽到队列** | **不支持**（队列面板只在批量视图）。仅能通过右键菜单"加入队列"。 |
| **双击图片** | 缩略图网格自动滚动到该图并高亮，**然后自动进入全屏看图** |
| **侧边栏（目录树）** | 左侧 240px（可拖拽调整 180~480px），完整文件系统树，懒加载，可收起。唯一目录切换入口。 |
| **侧边栏自动定位** | 切换目录时，侧边栏自动展开并高亮定位到该目录对应的树节点。 |

## 5.6 全屏看图模式细节 (Fullscreen Viewer Details)

| 项 | 决策 |
|----|------|
| **6a 视觉** | 全屏覆盖整个窗口，**隐藏**顶 Tab、底状态栏。 |
| **6b UI 元素** | 顶部悬浮精简工具条（上一张/下一张/缩放/退出/适应窗口），底部工具栏（旋转/翻页/缩放/编辑），鼠标静止 1.5 秒后自动隐藏，移回时再现。 |
| **6c 元信息** | 右下角小卡片显示：文件大小、格式、像素。不解析 EXIF（留口给未来版本）。与底部工具栏同进退。 |
| **6d 双击定位** | 缩略图自动滚动+高亮该图，**然后自动进入全屏**。 |
| **6e 旋转** | 底部工具栏旋转按钮（R 快捷键），顺时针 90° 步进。旋转后自动适应窗口。 |
| **6f 首次提示** | 首次进入全屏时底部中央淡入提示条（←/→/空格翻页、Esc 退出、+/- 缩放、R 旋转、E 编辑），5 秒后淡出。 |
| **6g 半区翻页** | 点击画布左 35% 区域 = 上一张，右 35% = 下一张，中间 30% 无操作。拖拽/缩放模式下不触发。 |
| **6h 快捷键** | Esc 退出 / ← 上一张 / → or 空格 下一张 / +/- 缩放 / 0 原始尺寸 / F 适应窗口 / R 旋转 / E 去单图编辑 |

## 5.7 批量视图 - 队列面板细节 (Batch View - Queue Panel)

| 项 | 决策 |
|----|------|
| **7a 队列项视觉** | 缩略图 + 文件名 + 状态图标（待处理 / 处理中 / 完成 / 失败） |
| **7b 排序** | 按加入时间先入在前。重复加入**忽略**。**不支持**手动拖拽排序。 |
| **7c 队列项操作**（每项右键菜单） | ① 移除 ② 在单图编辑中打开 ③ 复制文件路径 ④ **在资源管理器中显示** |
| **7d 面板顶部按钮** | "清空队列" + "打开输出目录" |
| **7e 处理进度显示** | 每项旁加进度条 / spinner，实时反映单图处理状态。 |

## 5.8 架构决策 (Architecture Decisions)

| 项 | 决策 |
|----|------|
| **状态管理** | **Zustand**（轻量、局部订阅、4 视图共享） |
| **持久化** | Zustand 持有运行时镜像 + 启动时从 `tauri-plugin-store` 读、写时双写 |
| **读目录** | **Rust 端**新增 `read_dir` 命令，扫描 + 过滤格式 + 读元数据 |
| **缩略图生成** | **Rust 端**新增 `make_thumbnail` 命令，返回 PNG bytes；前端用 base64/data URL 渲染 |
| **关联格式管理** | `tauri-plugin-store` 存勾选状态 + Rust 端单独写 Windows 注册表命令 |
| **批量命令** | 保留旧 `batch_process(input_dir, ...)`，**新增** `batch_process_queue(paths, ...)` |

## 6. 视图组件 (UI Components)

| 术语 | 定义 |
|------|------|
| **视图切换器 (View Switcher)** | 顶部 4 个 Tab/按钮。 |
| **缩略图网格 (Thumbnail Grid)** | 浏览视图主区域。**懒加载**生成缩略图。 |
| **全屏看图 (Fullscreen Viewer)** | 浏览视图的子视图，一次一张大图，可缩放/旋转/翻页。`Esc` 退出。 |
| **队列面板 (Queue Panel)** | **仅在批量编辑视图右侧出现**，默认展开可折叠。 |
| **视图工具栏 (View Toolbar)** | 视图顶部水平条。 |
| **设置子 Tab (Settings Sub-Tab)** | 设置视图内的 3 个子 Tab。 |

## 7. 应用启动与文件关联 (Launch & File Association)

### 7.1 启动路由

| 启动方式 | OS 行为 | 应用行为 |
|---------|---------|----------|
| **冷启动 (Cold Start)** | 用户从桌面图标启动，不带参数 | → 浏览视图，自动加载**上次的目录** |
| **带单图参数** | 双击 .jpg，OS 启动 PicCraft + 文件路径 | → 浏览视图，把文件所在目录设为当前目录，进入**全屏看图**并定位该图 |
| **带目录参数** | 右键目录"用图轻剪打开" | → 浏览视图，把该目录设为当前目录 |
| **右键"用图轻剪编辑"** | 右键图片 → 打开方式 → "用图轻剪编辑" | → **单图编辑视图**（不是浏览视图） |

> ⚠️ **关键区分**：
> - 双击图片 = 浏览视图（全屏看图模式）
> - 右键"用图轻剪编辑" = 单图编辑视图
> - 二者触发源不同（OS 默认行为 vs OS 打开方式菜单），路由不同

### 7.2 文件关联管理

- 在 **设置视图 → 设置子 Tab** 中，可以勾选要关联给 OS 的图片格式（jpg/jpeg/png/webp/bmp）
- 状态用 `tauri-plugin-store` 持久化
- **Rust 端单独写命令**直接操作 Windows 注册表，实时同步勾选状态

## 8. 范围 (Scope)

- 支持格式：jpg / jpeg / png / webp / bmp
- **不递归子目录**
- 不做：图片搜索、标签、EXIF 编辑、缩略图磁盘缓存

## 9. 经验教训 (Lessons Learned)

### Bug 教训

| # | 教训 | 来源 |
|---|------|------|
| 1 | **Konva Canvas + 绝对定位 overlay**：`react-konva` 的 `<Stage>` 内部 div 是 `position: relative`，作为同级定位元素在 DOM 后面出现时会盖在 overlay 之上。全屏看图的工具条、信息卡、加载提示、错误提示**一律要加 `z-10`** | 全屏工具条不可点击 |
| 2 | **Tauri 2 CSP scheme `http://` vs `https://`**：`convertFileSrc` 生成 `http://asset.localhost`（HTTP），并非 HTTPS。CSP `img-src 'self' https://asset.localhost` 不覆盖 HTTP。正确写法：`asset: http://asset.localhost https://asset.localhost` | 编译版全屏黑屏 |
| 3 | **静默吞错 = 自盲**：`img.onerror` 无 `console.error`、无 UI error state → 用户面对黑屏零反馈。每个错误回调必须同时有日志 + UI 提示 | 编译版全屏黑屏 |

### 架构教训

| # | 教训 | 来源 |
|---|------|------|
| 4 | **Rust 函数定义顺序不影响编译**：Rust 模块内函数定义在后、调用在前完全合法——不需要前向声明。`parse_from_iter` 在第 140 行定义、第 79 行调用，编译通过 | "Issue 1" 误报 |
| 5 | **`#[tauri::command]` + 同文件冲突**：Tauri 2 的 `#[tauri::command]` 宏会生成 `__cmd__X` 内部宏，与同文件其他 `generate_handler!` 引用冲突。解法：把有冲突的命令放 `image_ops` 子模块，用 `use crate::image_ops::xxx` 跨模块引用 | M1-A 编译踩坑 |
| 6 | **`SearchReplace` 工具不稳定**：报告"successfully"但实际文件未改。**必须 Read 验证每次 SearchReplace 的结果**。确实不稳定时用 `PowerShell -replace` 或 `Write` 整个文件 | M1-M4 多次踩坑 |
| 7 | **`Write` 工具截断**：超过一定长度（约 400 行）时 Write 会截断文件末尾。超长文件需 `Write` 后 `Add-Content` 续写 | M6-B 文件损坏 |

## 10. 不在范围 (Out of Scope)

- 图片搜索、标签、评分
- 缩略图缓存的磁盘持久化
- 远程图片
- 视频文件
- 单图编辑的撤销栈（除已实现的）
- Linux / macOS 的"右键打开方式"集成（本期只做 Windows）
